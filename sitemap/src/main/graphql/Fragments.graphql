fragment objectData on smb_objects {
    id
# TODO damned, we can't fetch the attribute translations in a performant way :-(
#    titles:attribute_translations(limit: 1, where: {
#        language: {lang: {_eq: "de"}}
#        attribute_key: {_eq: "ObjObjectTitleVrt"}
#    } ) {
#        value
#    }
}

fragment topicData on smb_topics {
    id
    titles:topics_translations(where: {language: {lang: {_eq: "de"}}} ) {
        value:title
    }
}

fragment tourData on smb_tours {
    id
    titles:tours_translations(where: {language: {lang: {_eq: "de"}}} ) {
        value:title
    }
}
